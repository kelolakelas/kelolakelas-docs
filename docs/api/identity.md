# Identity API

Implemented routes are registered in `kelolakelas-identity-service/cmd/server/main.go:103-131`; request binding structs and response branches are in `internal/delivery/http/handler/`.

| Operation | Key request schema | Result / notable errors |
|---|---|---|
| Register | `RegisterPayload`: email, password 6–72 bytes, first/last name max 255 characters, phone? max 50 characters, is_parent | 201 user record; does **not** issue token despite web action checking for one; 400 invalid/oversized input, 409 email conflict |
| Login | `LoginPayload`: email, password | 200 token + user + tenant_id; 401 invalid credentials |
| Platform login (`POST /api/v1/platform/auth/login`) | `LoginPayload`: email, password | 200 `data.token` (tenantless `is_platform_admin` JWT); 400 invalid payload, 401 invalid credentials/inactive assignment, 503 assignment store failure |
| Platform identity (`GET /api/v1/platform/me`) | Bearer platform token | 200 `data.user_id` after live assignment check; 401 invalid/expired token, 403 missing platform principal or revoked assignment, 503 store failure |
| Configuration inventory (`GET /api/v1/platform/configurations`) | Bearer platform token; required `environment` query | 200 metadata/status for all five applications; sensitive entries are `not_managed`, with no secret default or value; 400 invalid environment, 403 revoked assignment, 503 authorization store unavailable |
| Configuration history (`GET /api/v1/platform/configurations/{application}/{key}/history`) | Bearer platform token; required `environment` query | 200 non-secret version/report history; 400 unknown or sensitive key |
| Configuration request (`POST /api/v1/platform/configurations/{application}/{key}/versions`) | `environment`, `expected_version`, and either non-secret JSON scalar `value` or `rollback_version_id` | 201 new `requested` version with actor; 400 invalid/unknown/secret key or value; 409 stale version; no runtime application |
| Configuration report (`POST /api/v1/platform/configurations/reports`) | `version_id`, `status` (`applied`, `failed`, `rollback`) | 201 operator acknowledgement; 400 invalid status; 404 unknown version; not deployment proof. An `applied` report for `identity/platform/TENANT_REGISTRATION_OPEN` or `academic/platform/PUBLIC_CATALOG_OPEN` changes its effective policy; other entries remain record-only |
| Registration policy (`GET /api/v1/platform/registration-policy`) | Bearer platform token with active assignment | 200 `data.open` (effective boolean), `applied_version`, `desired_version`, `application`, `environment`, `key`; 403 unauthorized platform principal, 500 policy unavailable |
| Close/open registration (`POST /api/v1/platform/registration-policy/close` or `/open`) | Bearer platform token with active assignment; no body | 200 same effective-state fields after appending desired version, **not** immediate application: pending close can return `open: true`, pending open `open: false` until the corresponding version is reported `applied`; 409 stale concurrent version, 500 write/read failure |
| Public catalog policy (`GET /api/v1/platform/catalog-policy`) | Bearer platform token with active assignment | 200 effective `data.open`, `applied_version`, `desired_version`, `application: academic`, `environment: platform`, `key: PUBLIC_CATALOG_OPEN`; 403 unauthorized platform principal, 500 policy unavailable |
| Close/open public catalog (`POST /api/v1/platform/catalog-policy/close` or `/open`) | Bearer platform token with active assignment; no body | 200 effective policy after appending a desired version; a desired-only version with no applied report hides the catalog until acknowledgement. 409 stale concurrent version, 500 policy failure; class/tenant rows unchanged. Generic configuration history, report and rollback endpoints supply the audit and manual applied acknowledgement |
| Tenant register | `RegisterTenantRequest`: password 6–72 bytes, first/last/tenant name max 255 characters, phone/tenant_phone max 50 characters | 201 token/user/tenant; 400 invalid/oversized input, 409 name/email conflict |
| Invitation create/verify/register | `CreateInvitationPayload`; token query; invited-user payload | creation requires `member:invite` and returns 403 when absent, 400 for a foreign non-system role, 403 for the system Creator role, and 409 for an email already belonging to an account (without sending mail). Resending to an unregistered email replaces its unredeemed invitations in the tenant transactionally before creating the new token. A stored invitation answers **201 whether or not the email was delivered**: `data` contains `id`, `email`, `role_id`, `expires_at`, `status`, and `email_sent`, never `token`; its message states the delivery outcome (KEL-36). Validation handles absent/expired/used tokens, including revoked and superseded tokens. Delivery failures are logged with invitation and tenant IDs, never the token |
| Invitation list (`GET /api/v1/invitations`) | Bearer tenant token | 200 array of unredeemed invitations for the claim tenant, including expired entries marked `expired`; `member:invite` required (403). Response omits `token` |
| Invitation revoke (`DELETE /api/v1/invitations/{id}`) | Bearer tenant token; invitation UUID | 204 on revoke; 400 invalid UUID, 403 without `member:invite`, 404 for another tenant's, missing, or already-used invitation. Revoked tokens fail verify and register |
| Creator request create (`POST /api/v1/creator-requests`) | Bearer tenant token; `target_email` and/or `target_user_id`, required `reason` | 201 pending request without role assignment; live active Creator membership in the token's tenant required (403); 400 invalid target/reason; 409 duplicate pending request or target already Creator |
|| Creator request list (`GET /api/v1/creator-requests`) | Bearer tenant token | 200 requests scoped to token tenant, newest first; 403 without live Creator membership |
| Platform Creator request queue (`GET /api/v1/platform/creator-requests`) | Bearer platform token; identity active-platform assignment check | 200 `{status:"success", message, data:[...]}` for pending requests across tenants, oldest `created_at` first (then id), no pagination or status filter; fields: id, tenant_id, tenant_name, requester_user_id, target_email, target_user_id, reason, status, created_at, updated_at, decided_by, decided_at, rejection_reason. No tokens/hashes/member details; 401 invalid session, 403 revoked/non-platform admin, 500 repository error. Tenant list response is unchanged |
|| Creator request decide (`POST /api/v1/platform/creator-requests/{id}/approve`) | Bearer platform token; live active assignment rechecked in-transaction | 200 decided request; registered target granted Creator exactly once with audit row; unregistered target gets email-bound invitation, role only on valid acceptance; 400 bad UUID, 403 revoked/absent admin, 404 unknown request, 409 replayed/stale/already-Creator; invitation email failure logged, not fatal |
|| Creator request decide (`POST /api/v1/platform/creator-requests/{id}/reject`) | Bearer platform token; live active assignment rechecked in-transaction; required `reason` (1-2000 chars, trimmed) | 200 decided request with stored rejection reason and audit row; no membership or invitation change; 400 missing/oversize reason or bad UUID, 403 revoked/absent admin, 404 unknown request, 409 replayed decision |
| Members / tutors | pagination/query filters; UUID path | tenant context from the verified JWT claim only; 403 when the token carries no tenant claim, 401 when the claim is unusable, 400 malformed query/ID |
| Member role change (`PUT /api/v1/members/{id}/role`) | Bearer tenant token; `role_id` | 200 updated member (visible in `GET /api/v1/members/{id}`). `member:update` required through an active membership with the token's role (403 otherwise, checked before any lookup). Inside one transaction, before any write: 404 member not in the token's tenant; **409 "You cannot change your own member role"** when the target is the caller's own membership (matched on the `member_id` claim, or `user_id` for tokens without it); 409 "Role does not belong to tenant" for a role of another tenant or an unknown role; 403 "Creator role requires platform approval" for the system Creator target (KEL-94); 403 "Creator role cannot be changed" when the member's current role is the system Creator. A member on the system Teacher role, or on a tenant custom role, can be moved (KEL-79) |
| Roles / permissions | role DTOs | custom-role create/update/delete require `role:create`, `role:update`, and `role:delete`; tenant ownership and system-role restrictions are enforced in the role use case. Every permission decision is evaluated inside the caller's tenant: the assignment counts only when the role belongs to that tenant or is a system role (`roles.tenant_id IS NULL`), so a role lifted from another tenant is denied and a role deleted after the token was issued resolves no rows and is denied |
| Tenant settings/location | settings: name max 255 characters, phone? max 50; location: google_place_id? max 255 | settings and location mutation require `tenant:update` (403 when absent); 400 invalid/oversized input; 409 if settings rename matches another tenant's name case-insensitively, without mutation; own-name case-only rename is allowed. Optional geocode on address-only location update |

Invited-user registration also rejects passwords over 72 bytes and first/last names over 255 characters with the identity error envelope (400). **Implemented (KEL-88):** byte-length password guards precede registration use cases; settings name conflicts are checked before mutation (unique-constraint races remain outside this change). Evidence: identity PR #15, squash `cc9459680436ce5ac5d2bd246b718a53efb30fb4`; `internal/delivery/http/handler/{auth_handler,invitation_handler,tenant_handler,registration_limits_test}.go`, `internal/domain/tenant.go`, `internal/usecase/{tenant_usecase,tenant_settings_conflict_test}.go`, `internal/repository/{tenant_repository,tenant_name_test}.go`.

Every tenant-scoped route resolves its tenant from the `tenant_id` claim of the
validated access token (`internal/delivery/http/handler/tenant_context.go`,
KEL-16). A caller whose token carries no tenant claim — all parents, and any
user without an active membership — is rejected with **403 before the use case
is invoked**, so no repository query runs and no other tenant's data is read or
written. A `X-Tenant-ID` header is ignored entirely and is no longer part of any
published operation in `docs/swagger.json`; sending the caller's own tenant in
the header changes nothing, because the claim always wins.

Status codes on these routes are deliberately distinct:

- **403** — the token is valid but grants no tenant scope for this operation.
- **401** — the token was accepted by the middleware but carries an unusable
  (unparseable) tenant claim; a missing or nil claim is 403, not 401.
- **400** — malformed request input such as an unparseable UUID in the path or
  query, unchanged from before.

All payload fields should be confirmed against current Go domain structs or `docs/swagger.json` before client generation. The in-web copied Swagger documents are stale `Tutorin` artifacts and should not be used as the primary contract.
